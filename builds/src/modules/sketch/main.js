var Input;

global.runtime.helpers.MakeGlobalNamespaceFromString('engine.input', global);

Input = (function() {
  function Input() {}

  return Input;

})();

define(['modules.sketch/class.particle', 'modules.sketch/ui.jquery', 'modules.sketch/output.synemania'], function() {});
